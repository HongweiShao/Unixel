# 医学影像处理软件（Unixel）界面设计文档

> 版本：v1.0 ｜ 日期：2026-08-18 ｜ 范围：前端 GUI（Vue 3 + Tauri 2 WebView）
> 配套文档：`产品需求规格书.md`、`产品技术方案.md`

---

## 1. 概述

### 1.1 产品定位
医学影像处理软件是一款**轻量、可本地离线运行**的桌面医学影像查看与处理工具，覆盖 DICOM / 常规图像 / NIfTI 体数据 / HTJ2K 的导入、窗位调校、格式转换与批量导出闭环。前端为 Tauri 内嵌 WebView（Vue 3 + TypeScript + Vite）。

### 1.2 设计目标
- **零学习成本**：单窗口、单主操作（"打开文件"），核心交互即"选图→调窗→导出"。
- **影像优先**：画布占据主区域，工具栏/元信息栏为窄条、不挤占阅片空间。
- **实时反馈**：窗位、窗宽、缩放、帧切换均实时重绘，无确认步骤。
- **深色优先**：医学阅片以暗背景减少眩光、突出灰度影像。

### 1.3 本版界面范围
| 模块 | 文件 | 说明 |
|------|------|------|
| 应用外壳 / 布局 / 状态 | `src/App.vue` | Header、主区占位、批量横幅、打开/导出流程编排 |
| 影像查看器 | `src/components/Viewer.vue` | 窗位/窗宽/帧控制、画布交互、单帧导出 |
| 窗映射算法 | `src/windowing.ts` | HU→灰度映射、MONOCHROME1 反转 |
| 数据模型 | `src/types.ts` | `DicomMeta` / `DicomImage` / `NiftiMeta` 等接口 |
| 主题变量 | `src/style.css` | 配色 / 前景 / 边框 / 强调色 CSS 变量 |
| 入口 | `src/main.ts`、`index.html` | 挂载 `#app`、页面标题 |

---

## 2. 整体布局

单窗口纵向三段式：顶部 Header（固定）、中部主内容区（自适应填满）、可选的批量导出结果横幅（插入 Header 与主区之间）。

```
┌──────────────────────────────────────────────────────────────┐
│ Header                                                        │
│  Unixel · 医学影像处理软件    R+T+V3 · DICOM/图像/NIfTI/HTJ2K   [打开文件] [批量导出] │
├──────────────────────────────────────────────────────────────┤
│ （批量导出结果横幅，仅导出后出现）                              │
├──────────────────────────────────────────────────────────────┤
│ 主内容区 main                                                  │
│  ├─ 占位态（无图）：[打开文件] [载入示例体数据] 支持格式提示    │
│  └─ 查看态：                                                  │
│      ┌─ MPR 栏（仅 NIfTI）轴向/冠状/矢状  体尺寸 NxNxN ─┐       │
│      ├─ Viewer 工具栏：文件名 窗位[===o===] 窗宽[==o==] 帧 ─┤   │
│      ├─ Viewer 导出栏：导出[格式▼] 质量[==o==] [导出当前帧] ┤   │
│      ├─ Viewer 元信息栏：WxH · 帧数 · bit · 光度 · slope/intercept│
│      └─ 画布 canvas（黑底，鼠标滚轮缩放 / 拖拽平移）          │
└──────────────────────────────────────────────────────────────┘
```

- `header`：`display:flex`、底部 1px 边框、背景 `--panel`。
- `main`：`flex:1; min-height:0`，承载占位或查看器，填满剩余高度。
- 画布 `.canvas`：`flex:1; width:100%; background:#000`，是阅片主舞台。

---

## 3. 界面状态机

查看器组件 `Viewer` 通过 `:key` 强制重挂载刷新（见 §8）。应用级状态由 `App.vue` 的响应式变量驱动：

| 状态 | 触发条件 | 界面表现 |
|------|----------|----------|
| 初始占位 | `!imageView && !niftiView` | 居中显示"打开文件""载入示例体数据"按钮 + 格式提示 |
| 加载中 | `loading === true` | Header"打开文件"按钮禁用、文案变"解码中…" |
| 错误 | `error !== null` | 占位态下方红字 `⚠ {error}`（后端错误字符串或前端异常） |
| 正常查看（DICOM/图像/HTJ2K） | `imageView` 有值 | 渲染 `Viewer`（`:key="img-{openSeq}"`） |
| 正常查看（NIfTI MPR） | `niftiRender` 有值 | 渲染 `Viewer`（`:key="nii-{openSeq}-{niftiKey}"`）+ MPR 切换栏 |
| 示例数据 | `meta.path` 以 `mock` 开头 | 导出按钮禁用，提示"示例数据不可导出" |
| 批量导出完成 | `batchResult` 有值 | 顶部绿色横幅：成功 N 张 / 失败 M 张 + 失败清单 + 关闭 |

> **重开刷新机制**：每次成功打开文件，`openSeq` 自增作为 `Viewer` 的 `:key`（`App.vue`）。
> 若不加重挂载，Vue 会复用同一 `Viewer` 实例、仅更新 `meta/frames` prop，但 `render()` 仅由
> `watch([wc,ww,frameIndex,zoom,pan])` 与 `onMounted` 触发，prop 变化不重绘 → 旧图残留。
> 重挂载保证窗位/帧/缩放/平移全部按新文件重置并立即重绘。

---

## 4. 顶部工具栏（Header）

| 元素 | 类型 | 行为 |
|------|------|------|
| 标题 `Unixel · 医学影像处理软件` | 文本 | 左对齐品牌标题 |
| 副标题 | 文本 | 技术栈与格式一句话说明（`--fg-dim`） |
| **打开文件**（primary） | 按钮 | 调 `openFile()`：弹出文件选择框 → 按扩展名路由 `load_dicom/load_image/load_nifti/load_htj2k` → 解码填充 `imageView`/`niftiView`；`loading` 时禁用、文案"解码中…" |
| **批量导出** | 按钮 | 调 `batchExport()`：先多选文件 → 选输出目录 → 调 `batch_export` → 结果横幅 |

文件过滤器（前后端一致）：`dcm/dicom`、`png/jpg/jpeg/tif/tiff`、`nii/nii.gz`、`j2c/jph`。

---

## 5. 查看器（Viewer）组件

由 `src/components/Viewer.vue` 实现。接收两个 prop：`meta: DicomMeta`、`frames: Float32Array[]`（每帧 HU 数组，长度 `width*height`）。

### 5.1 工具栏（toolbar）
单行、可换行、背景 `--panel`、底部 1px 边框。

| 控件 | 绑定 | 范围 | 说明 |
|------|------|------|------|
| 文件名 | `meta.filename`（title=`meta.path`） | — | 左对齐、超长省略号，便于确认当前打开的是哪张 |
| **窗位** slider | `wc`（v-model.number） | `huMin` ~ `huMax`，step 1 | 窗中心；变化即触发 `render()` |
| **窗宽** slider | `ww`（v-model.number） | 1 ~ `huMax-huMin`，step 1 | 窗跨度；变化即触发 `render()` |
| **帧** slider | `frameIndex`（仅 `frames>1` 显示） | 0 ~ `frames-1` | 多帧/序列导航，显示 `当前/总数` |
| **重置** | 按钮 | — | 恢复 `zoom=1; pan={0,0}; wc=windowCenter; ww=windowWidth; frameIndex=0` |
| **关闭** | 按钮 | — | `emit('close')` → `App.clearView()` 清空视图回占位态 |
| **缩放** | 文本 | — | 实时显示 `zoom.toFixed(2)x` |

### 5.2 导出栏（export-bar）
| 控件 | 说明 |
|------|------|
| 导出格式 `select` | `png` / `jpeg` / `htj2k`；选择 `jpeg` 时显示质量滑杆（10–100） |
| **导出当前帧** | 取当前帧 HU 字节 → 调后端 `export_frame`（按当前 `wc/ww` 做窗映射与编码）→ 弹保存框；`mock` 数据禁用 |
| 提示文本 | 示例数据显示"示例数据不可导出"；导出成功显示绿色"已导出：{path}"，失败显示红字错误 |

### 5.3 元信息栏（meta-bar）
单行 11px 灰字：`宽×高 · 帧数 · bitsStored bit · 光度(photometric) · slope intercept`。

### 5.4 画布与交互
- 画布固定 `800×600`，CSS `flex:1; width:100%` 撑满，黑底。
- **滚轮缩放**：`deltaY<0` 放大 1.1×、否则缩小，限制在 0.1×–8×（`onWheel`，`.prevent` 阻止页面滚动）。
- **拖拽平移**：`mousedown→mousemove` 更新 `pan`，`mouseup/mouseleave` 结束。
- 渲染流程（`render()`）：取 `frames[frameIndex]` → 窗映射写入复用 `rgba` 缓冲 → 离屏 canvas 原分辨率 `putImageData` → 主 canvas `drawImage` 居中并按 `zoom` 缩放（最近邻 `imageSmoothingEnabled=false`）。
- 空帧/尺寸不匹配（`frame.length===0` 或 `rgba.length!==frame.length*4`）安全跳过或重建缓冲，避免 `ImageData` 尺寸异常抛错导致画布空白。

### 5.5 窗映射（windowing.ts）
`applyWindow(frame, wc, ww, rgba, photometric)`：按窗 `wc/ww` 将 HU 线性映射到 0–255；`MONOCHROME1` 做灰度反转。滑块边界取自 `meta.huMin/huMax`（后端解码已含 Modality LUT 的 HU 区间）；窗值单位为 HU（后端 `decode_dicom_file` 在有 Rescale 时已把 `WindowCenter/Width` 换算到 HU）。

---

## 6. MPR 多平面重建视图（NIfTI）

NIfTI 体数据走独立渲染分支（`niftiRender` computed）：将 `[x][y][z]` 体按轴重排为多帧 `frames` 后复用 `Viewer`。

- **MPR 切换栏**（`.mpr-bar`，仅 `niftiView` 时显示）：轴向 / 冠状 / 矢状 三按钮（`active` 高亮当前轴）、右侧显示体尺寸 `nx × ny × nz`。
- 切换轴 → `niftiView.axis` 变化 → `niftiKey` 变化 → `Viewer` `:key` 变化 → 重挂载并重排帧。
- NIfTI 的 `meta` 由 `extractNifti()` 结果 + `huMin/huMax` 构造（`windowCenter=(huMin+huMax)/2`、`windowWidth=huMax-huMin`）。

---

## 7. 批量导出流程

1. 点"批量导出" → 弹出**多选**文件框（同 §4 过滤器）。
2. 选中后弹出**目录**选择框（"选择批量导出目录"）。
3. 调后端 `batch_export({paths, format, quality, outDir})`，`batchBusy` 期间按钮禁用、文案"导出中…"。
4. 返回 `BatchResult{ok, failed[]}` → Header 下方绿色横幅展示成功数、失败数（含失败清单 `path—error`）与"关闭"按钮。

---

## 8. 视觉规范（Theme Tokens）

来自 `src/style.css` 的 `:root` CSS 变量，深色主题：

| Token | 值 | 用途 |
|-------|-----|------|
| `--bg` | `#0d1117` | 页面/画布外背景 |
| `--panel` | `#161b22` | Header、各工具栏、导出栏、元信息栏背景 |
| `--border` | `#30363d` | 分隔线/边框 |
| `--fg` | `#e6edf3` | 主文字/文件名 |
| `--fg-dim` | `#8b949e` | 副标题/标签/提示/元信息 |
| `--accent` | `#2f81f7` | 主操作按钮（打开文件）、激活态、导出按钮、滑块 accent |

通用排版：
- 字体：`-apple-system, "Segoe UI", "Microsoft YaHei", sans-serif`。
- 按钮：透明底 + `--border` 描边、圆角 6px、12–13px；主操作为 `--accent` 实底白字。
- 滑块控件：12px 标签 + 滑杆 + 固定最小宽数值（窗位 44px、质量 28px 右对齐）。
- 画布：黑底（`#000`），强调灰度影像对比。

状态色（横幅/提示）：
- 成功：`#2e9e5b`（导出成功文案）
- 失败/错误：`#e5484d`（导出失败、错误提示）
- 批量横幅底：`#16331f`、文字 `#c8f0d4`、失败清单 `#e5a3a3`

---

## 9. 交互与导航流程

```
[打开文件] ──选择──▶ 解码(loading) ──成功──▶ Viewer(查看/调窗/导出)
                         │                       │
                         └──失败──▶ 占位态红字错误
[批量导出] ──多选+选目录──▶ batch_export(导出中) ──▶ 结果横幅
[关闭] ──▶ clearView ──▶ 占位态
重开文件 ──▶ openSeq++ ──▶ Viewer 重挂载(刷新+重置状态)
```

- 单文件查看与批量导出共用"打开文件/批量导出"入口，互不影响。
- 任意时刻只有一个查看器（DICOM/图像/HTJ2K 走 `imageView`，NIfTI 走 `niftiRender`）。

---

## 10. 已知限制与后续演进

- **仅未压缩 DICOM**：压缩传输语法（JPEG2000 等）暂未支持，打开会走后端错误提示。
- **批量导出为"每文件默认窗"**：尚无序列分组（SeriesInstanceUID）面板与逐文件窗预览。
- **窗映射在 CPU（前端 JS）**：超大幅面（4K×4K）有性能压力，后续可下放 Rust/WASM 或 GPU。
- **示例数据（mock）**仅用于前端联调，不可导出。
- 自适应/可访问性：当前为固定桌面布局，未做移动端适配与键盘可达性（滑块已可键盘操作，但画布缩放/平移依赖鼠标）。

---

## 附录 A：关键数据模型（`src/types.ts`）

```ts
interface DicomMeta {
  path: string; filename: string;
  width: number; height: number; frames: number;
  bitsStored: number;
  pixelRepresentation: number; // 0=unsigned, 1=signed
  slope: number; intercept: number;
  windowCenter: number; windowWidth: number;
  photometric: string;          // MONOCHROME1/2, RGB…
  huMin: number; huMax: number; // 滑块边界，已含 Modality LUT
}
interface DicomImage { meta: DicomMeta; pixelBytes: Uint8Array | string | number[]; }
interface NiftiMeta  { path: string; filename: string; dims: [number,number,number]; huMin: number; huMax: number; }
interface NiftiVolume { meta: NiftiMeta; voxelBytes: Uint8Array | string | number[]; }
interface BatchResult { ok: number; failed: { path: string; error: string }[]; }
```

> `pixelBytes`/`voxelBytes` 经 Tauri v2 JSON IPC 时为 `number[]`（serde_bytes 退化），前端 `decodePixelBytes` 统一兼容 `number[] / Uint8Array / base64 string` 三种形态。
