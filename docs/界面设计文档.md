# 医学影像处理软件（Unixel）界面设计文档

> 版本：v1.9 ｜ 日期：2026-08-18 ｜ 范围：前端 GUI（Vue 3 + Tauri 2 WebView）
> 配套文档：`产品需求规格书.md`、`产品技术方案.md`
> v1.1 变更：顶部改为菜单栏；新增底部状态栏与右侧工具栏；新增关于对话框、标签详情对话框、多图下拉切换。
> v1.2 变更：菜单栏去除品牌名；一级菜单「关于」改为「帮助」（子项仍为「关于」）；状态栏「详细」按钮改名为「更多信息」并紧跟文件名；标签详情对话框改为单行显示、溢出行提供「展开/收起」；右侧工具栏拆为「视图」「导出」两组，缩放角标移至画布右下角。
> v1.3 变更：视图工具组移除「关闭」按钮及 `emit('close')`/`clearView` 逻辑；状态栏顺序改为「文件名 → 尺寸 · 帧数 → 更多信息」（更多信息紧跟元信息后）；标签详情对话框值列 `max-width` 收至 240px，操作列（`展开/收起`）设为右侧 `sticky` 钉住，对话框变窄时始终可见。
> v1.4 变更：关于对话框新增英文品牌名「Unixel」（中文名下方副标题，`.en-name`，弱化显示）；同步更新 §7 关于对话框字段表。
> v1.5 变更：① 占位态新增「从文件夹导入」按钮（与菜单栏「文件 → 从文件夹导入」等价，直接调 `importFolder()`）；② 查看器右侧工具栏新增「导航」组（合并单列表：多帧时先列帧、当前文件属于多文件系列时再列该系列文件，可垂直滚动、点击切换）；③ 状态栏文件名下拉按 `SeriesInstanceUID` 分组（`<optgroup>`），组内按空间位置排序；④ 标签详情对话框关键字后加圆形「?」图标 hover 弹出该标签/值说明（仅 DICOM）、右上角「×」关闭、新增 JSON/CSV 导出；⑤ 后端 `ImageInfo` 扩展系列字段并由 `list_folder_images` 按几何位置分组排序，新增 `file_series_info`/`export_tags` 命令，`file_tags` 每行增 `description`（DICOM 为 VR 含义，其它类型留空不显示问号）。
> v1.6 变更：**移除查看器右侧工具栏的「导航」组**。该组（合并单列表：多帧列帧、当前文件属多文件系列时列同系列文件）已删除：`Viewer` 移除 `navItems`/`onNavClick`、`seriesFiles`/`activeId` props、`selectFile` emit 及相关样式；`App.vue` 移除对 `Viewer` 的 `:series-files`/`:active-id`/`@select-file` 绑定及失效的 `seriesFiles` computed。多帧导航改由「视图」组的「帧」滑块承担，系列内切换改由状态栏按系列分组的下拉完成（见 §6），能力不丢。状态栏 optgroup 分组、`file_series_info`/`group_and_sort_series` 后端保留（仍服务状态栏分组）。
> v1.7 变更：① **文件夹导入重构为递归扫描 + Study/Series 两级分组**：新增后端 `scan_folder_series(dir)`（递归遍历所有支持文件，DICOM 按 `StudyInstanceUID → SeriesInstanceUID` 两级聚合，非 DICOM 归入「其他影像文件」分组）与 `load_series_files(paths)`（仅加载所选序列的文件）；目录下仅一个 Series 时直接打开，多个时弹出「序列选择对话框」（按 Study 分组、Series 行内显示序列日期/患者姓名，单选后仅加载该序列）。② **多帧竖向滚动条**：位于画布右侧、工具栏左侧的独立竖条（非工具栏内、无导航组），点击帧号快速切换。③ 标签详情「?」悬停说明升级为「标签含义解释 + 常见取值」（`tag_explanation` 释义表，缺省回退 VR 含义）。④ **导出格式选择移入系统保存框**：`save` 带 JSON/CSV 双过滤器，按扩展名推断格式，对话框内不再有格式下拉。⑤ 修复「搜索框与关闭叉重叠」：详情头部右侧预留内边距、搜索框收窄。
> v1.8 变更：① 标签详情「?」悬停说明**去掉「值」信息**，释义内容改为参考 innolitics（dicom.innolitics.com/ciods）风格的中文标签解释（含义 + 常见取值），`tag_explanation` 释义表重写并扩充覆盖常见 DICOM 标签。② 右侧竖向滚动条交互改为**纯滚动条**：去掉左侧帧号/序号按钮，滚动条位置对应图像序号（多帧帧号 / 同系列切片序号），拖动即直接切换对应位置的图像。
> v1.9 变更：**修复滚动切换图像闪烁**。去掉 `Viewer` 的 `:key` 重挂载；`currentView` 由 computed 改为 ref，切到未解码切片时**保留上一帧显示**、后台解码完成再无感替换（Viewer 始终常驻、currentView 永不置 null → 不再卸载/重挂）；用 `loadSeq` 令牌丢弃快速拖动时的过期解码结果；`Viewer` 新增对 `meta`/`frames` 的监听，切片切换时**在位重绘**（不重置缩放）。滚动切换顺滑无闪烁。

---

## 1. 概述

### 1.1 产品定位
医学影像处理软件是一款**轻量、可本地离线运行**的桌面医学影像查看与处理工具，覆盖 DICOM / 常规图像 / NIfTI 体数据 / HTJ2K 的导入、窗位调校、格式转换与批量导出闭环。前端为 Tauri 内嵌 WebView（Vue 3 + TypeScript + Vite）。

### 1.2 设计目标
- **零学习成本**：单窗口、单主操作（"打开文件"），核心交互即"选图→调窗→导出"。
- **影像优先**：画布占据主区域，菜单/工具/状态为窄条、不挤占阅片空间。
- **实时反馈**：窗位、窗宽、缩放、帧切换均实时重绘，无确认步骤。
- **深色优先**：医学阅片以暗背景减少眩光、突出灰度影像。

### 1.3 本版界面范围
| 模块 | 文件 | 说明 |
|------|------|------|
| 应用外壳 / 布局 / 状态 | `src/App.vue` | 顶部菜单栏、主区占位、右侧工具栏承载、底部状态栏、关于/详情对话框、多图模型、打开/导入/导出流程编排 |
| 影像查看器 | `src/components/Viewer.vue` | 画布交互、单帧导出；**画布右侧帧滚动条**（多帧时竖向列出帧号、点击切换，位于画布与工具栏之间、非工具栏内）；**右侧竖向工具栏**（窗位/窗宽/帧/缩放/重置/导出） |
| 窗映射算法 | `src/windowing.ts` | HU→灰度映射、MONOCHROME1 反转 |
| 数据模型 | `src/types.ts` | `DicomMeta` / `DicomImage` / `NiftiMeta` / `ImageInfo` / `FileTags` 等接口 |
| 主题变量 | `src/style.css` | 配色 / 前景 / 边框 / 强调色 CSS 变量 |
| 入口 | `src/main.ts`、`index.html` | 挂载 `#app`、页面标题 |

---

## 2. 整体布局

单窗口**纵向四段式**：顶部菜单栏（固定）、中部主内容区（自适应填满、左侧画布 + 右侧工具栏）、底部状态栏（固定）、可选的批量导出结果横幅（插入菜单栏与主区之间）。

```
┌──────────────────────────────────────────────────────────────────┐
│ 菜单栏 menubar                                                       │
│  ☰ 文件   ☰ 帮助                                                     │
│   ├ 打开文件…                                                         │
│   ├ 从文件夹导入…                                                     │
│   └ 批量导出…                          (帮助 → 关于 → 弹出关于对话框) │
├──────────────────────────────────────────────────────────────────┤
│ （批量导出结果横幅，仅导出后出现）                                      │
├──────────────────────────┬─────────────┬────────────────────────────┤
│ 主内容区 main              │ 帧滚动条     │ 右侧工具栏 sidebar-toolbar   │
│  ├─ 占位态（无图）：        │ frame-strip │  ┌ 视图   [重置]             │
│  │  [打开文件]             │ (多帧时)    │  │  窗位  [===o===]           │
│  │  [载入示例体数据]        │  1          │  │  窗宽  [==o==]             │
│  │  [从文件夹导入]          │  2          │  │  帧    [==o==] (多帧时)    │
│  │  支持格式提示            │  3 … 390   │  └──────────────             │
│  └─ 查看态：                │ (滚轮切换)  │  ┌ 导出                      │
│     ┌─ MPR 栏（仅 NIfTI）轴/冠/矢 │          │  │  格式  [▼]                │
│     └─ 画布 canvas（自适应尺寸，  │          │  │  质量 [==o==] (jpeg 时)    │
│         滚轮缩放 / 拖拽平移，     │          │  │  [ 导出当前帧 ]           │
│         右下角缩放角标 1.00x）   │          │  └──────────────             │
├──────────────────────────┴─────────────┴────────────────────────────┤
│ 状态栏 statusbar                                                    │
│  📄 文件名[▼按系列分组下拉]  [ 更多信息 ]   尺寸 WxH   帧 1/390     │
│     └ optgroup「CT Series 1 · …xxxx」：CT000.dcm / CT001.dcm …      │
└──────────────────────────────────────────────────────────────────┘
```

- `menubar`：`display:flex`、底部 1px 边框、背景 `--panel`；菜单项 hover/展开态高亮。
- `main`：`flex:1; min-height:0`，内部 `display:flex` —— 画布区 `flex:1` + 右侧工具栏固定宽。
- `sidebar-toolbar`：竖向排列控件，`width` 固定（约 200px），背景 `--panel`、左侧 1px 边框。
- `canvas`：绘图缓冲随容器尺寸变化（`ResizeObserver` + `devicePixelRatio`），不再写死 800×600；CSS `flex:1; width:100%`，黑底。
- `statusbar`：`display:flex`、顶部 1px 边框、背景 `--panel`、11–12px 灰字。

---

## 3. 界面状态机

查看器组件 `Viewer` 通过 `:key` 强制重挂载刷新（见 §6）。应用级状态由 `App.vue` 的响应式变量驱动：

| 状态 | 触发条件 | 界面表现 |
|------|----------|----------|
| 初始占位 | `imageList` 为空 | 居中显示"打开文件""载入示例体数据""**从文件夹导入**"三个按钮 + 格式提示（"从文件夹导入"等价于菜单栏「文件 → 从文件夹导入」） |
| 加载中 | `loading === true` | 菜单项禁用、状态栏/占位区提示"解码中…" |
| 错误 | `error !== null` | 占位态下方红字 `⚠ {error}`（后端错误字符串或前端异常） |
| 正常查看（DICOM/图像/HTJ2K） | `imageList` 中激活项 `view` 有值 | 渲染 `Viewer`（`:key="img-{openSeq}"`） |
| 正常查看（NIfTI MPR） | 激活项 `view` 为 NIfTI 体 | 渲染 `Viewer`（`:key="nii-{openSeq}-{niftiKey}"`）+ MPR 切换栏 |
| 多图列表 | `imageList.length > 1` | 状态栏文件名处出现下拉，列出已导入图像，选中即切换 |
| 示例数据 | `meta.path` 以 `mock` 开头 | 导出按钮禁用，提示"示例数据不可导出" |
| 批量导出完成 | `batchResult` 有值 | 顶部绿色横幅：成功 N 张 / 失败 M 张 + 失败清单 + 关闭 |

### 多图模型（v1.1 新增）
`App.vue` 维护 `imageList: { id, info: ImageInfo, view: ImageView | null }[]` 与 `activeId`。
- **单张"打开文件"**：替换整个列表为单张（独占显示，不进多图下拉）。
- **"从文件夹导入"**：后端 `scan_folder_series(dir)` **递归**遍历目录下所有支持文件，DICOM 按 `StudyInstanceUID → SeriesInstanceUID` 两级聚合，非 DICOM 归入「其他影像文件」分组；若仅一个 Series 则直接打开，否则前端弹出「序列选择对话框」，用户选定后调用 `load_series_files(paths)` **仅加载该序列**（替换 `imageList`），列表 >1 张时状态栏显示下拉。
- **切换**：选中下拉项 → 若其 `view` 尚未加载，则按扩展名调 `load_*` 懒加载像素；切换 `activeId` → `openSeq` 自增 → `Viewer` 重挂载刷新。
- **重开刷新机制**：每次激活项变化（打开/切换/导入），`openSeq` 自增作为 `Viewer` 的 `:key`。若不加重挂载，Vue 复用同一实例、仅更新 prop，但 `render()` 仅由 `watch` 与 `onMounted` 触发，旧图残留。

---

## 4. 顶部菜单栏（Menu Bar）

| 菜单 | 菜单项 | 行为 |
|------|--------|------|
| **文件** | 打开文件… | 调 `openFile()`：弹出文件选择框 → 按扩展名路由 `load_dicom/load_image/load_nifti/load_htj2k` → 解码后**替换**列表为单张 |
| | 从文件夹导入… | 调 `importFolder()`：选目录 → 后端 `scan_folder_series(dir)` 递归扫描并按 Study/Series 两级分组 → 仅一个系列直接打开，多个则弹「序列选择对话框」选定后 `load_series_files` 仅加载该序列 |
| | 批量导出… | 调 `batchExport()`：多选文件 → 选输出目录 → 调 `batch_export` → 结果横幅 |
| **帮助** | 关于 | 弹出**关于对话框**（见 §7） |

- 菜单展开态带遮罩，点击空白处/菜单外自动收起。
- 文件过滤器（前后端一致）：`dcm/dicom`、`png/jpg/jpeg/tif/tiff`、`nii/nii.gz`、`j2c/jph`。

---

## 5. 查看器（Viewer）组件

由 `src/components/Viewer.vue` 实现。接收两个 prop：`meta: DicomMeta`、`frames: Float32Array[]`（每帧 HU 数组，长度 `width*height`）。
布局为「**画布（左，自适应） + 右侧竖向工具栏（sidebar-toolbar）**」，原顶部工具栏与元信息栏已移除（元信息上移至 §6 状态栏）。

### 5.1 右侧工具栏（sidebar-toolbar）

工具栏竖向分为两个分组（`section.group`）：**视图**（组标题最右侧放置 `重置` 迷你按钮）→ **导出**。

> v1.6 起移除原「导航」组（多帧列帧 / 同系列列文件的合并列表）。多帧帧间导航改由「视图」组的「帧」滑块承担；同系列文件切换改由 §6 状态栏按系列分组的下拉完成。
> **v1.7–v1.9 多帧/序列快速切换**：画布与右侧工具栏之间出现独立「竖向滚动条」（`frame-scroll`，宽约 14px），**位置对应图像序号**（多帧帧号 / 同系列切片序号），拖动或滚轮即直接切换到对应位置的图像，**无左侧序号按钮**。该滚动条**不属工具栏、不形成「导航」组**。**v1.9 起 Viewer 组件常驻不复挂载**：切帧/切切片时为「在位重绘」，切到未解码切片时保留上一帧、后台解码完成再无感替换，故滚动/拖动切换图像**不闪烁**。

**「视图」组**（组标题最右侧放置 `重置` 迷你按钮）
| 控件 | 绑定 | 范围 | 说明 |
|------|------|------|------|
| **窗位** slider | `wc`（v-model.number） | `huMin` ~ `huMax`，step 1 | 窗中心；变化即触发 `render()` |
| **窗宽** slider | `ww`（v-model.number） | 1 ~ `huMax-huMin`，step 1 | 窗跨度；变化即触发 `render()` |
| **帧** slider | `frameIndex`（仅 `frames>1` 显示） | 0 ~ `frames-1` | 多帧/序列导航，显示 `当前/总数` |
| **重置**（组内标题右侧） | 按钮 | — | 恢复 `zoom=1; pan={0,0}; wc=windowCenter; ww=windowWidth; frameIndex=0` |

**「导出」组**
| 控件 | 绑定 | 范围 | 说明 |
|------|------|------|------|
| 导出格式 `select` | `exportFormat` | `png` / `jpeg` / `htj2k`（HTJ2K 默认 `.jph`） | 选 `jpeg` 时显示质量滑杆 |
| 质量 slider | `exportQuality` | 10–100（仅 jpeg） | JPEG 质量 |
| **导出当前帧** | 按钮 | — | 取当前帧 HU 字节 → 调后端 `export_frame`（按当前 `wc/ww` 窗映射与编码）→ 弹保存框；`mock` 数据禁用 |

> 原「缩放」滑杆已移除，缩放交互改为滚轮 + 拖拽平移（见 §5.2）；当前缩放比例以画布右下角角标 `1.00x` 实时显示（`.zoom-badge`，`pointer-events:none`）。

### 5.2 画布与交互
- 画布绘图缓冲随容器尺寸变化：首次加载 `fitToView()` 等比居中铺满可视区（仅适配一次，之后保留手动缩放）；`ResizeObserver` 监听容器尺寸变化重绘；卸载时 `disconnect()` 清理。
- 高分屏清晰度：缓冲尺寸乘 `devicePixelRatio`，`render()` 中 `dw/dh` 同步乘 `dpr`。
- **滚轮缩放**：`deltaY<0` 放大 1.1×、否则缩小，限制 0.1×–8×（`onWheel`，`.prevent` 阻止页面滚动）。
- **拖拽平移**：`mousedown→mousemove` 更新 `pan`，`mouseup/mouseleave` 结束。
- 渲染流程（`render()`）：取 `frames[frameIndex]` → 窗映射写入复用 `rgba` 缓冲 → 离屏 canvas 原分辨率 `putImageData` → 主 canvas `drawImage` 居中并按 `zoom` 缩放（最近邻 `imageSmoothingEnabled=false`）。
- 空帧/尺寸不匹配（`frame.length===0` 或 `rgba.length!==frame.length*4`）安全跳过或重建缓冲，避免 `ImageData` 尺寸异常抛错导致画布空白。

### 5.3 窗映射（windowing.ts）
`applyWindow(frame, wc, ww, rgba, photometric)`：按窗 `wc/ww` 将 HU 线性映射到 0–255；`MONOCHROME1` 做灰度反转。滑块边界取自 `meta.huMin/huMax`（后端解码已含 Modality LUT 的 HU 区间）；窗值单位为 HU（后端 `decode_dicom_file` 在有 Rescale 时已把 `WindowCenter/Width` 换算到 HU）。

---

## 6. 底部状态栏（Status Bar，v1.1 新增）

固定在主区下方，`display:flex`、顶部 1px 边框、背景 `--panel`。

| 元素 | 来源 | 说明 |
|------|------|------|
| 文件名 | 激活项 `info.filename` | 左对齐；当 `imageList.length > 1` 时渲染为 `<select>` 下拉。**按系列分组（v1.5）**：前端 `groupedImages`（按 `info.seriesGroup` 聚合）渲染 `<optgroup>`；有 `series_group` 的文件归入对应 optgroup（标题取 `series_label`，如「CT Series 1 · …a1b2c3d4」），无系列的单文件直接作为顶层 `<option>`。组内文件已按空间位置排序（见附录 B `group_and_sort_series`）。选中即切换激活项 |
| 尺寸 | `info.width × info.height` | 当前图像像素尺寸 |
| 帧数 | `info.frames` | 当前帧数（多帧/序列显示） |
| 位深/光度 | `info.bitsStored` / `info.photometric` | 主元信息摘要 |
| **更多信息** 按钮 | — | **紧跟在尺寸·帧数之后**，调后端 `file_tags(path)` → 弹出**标签详情对话框**（见 §8） |

> 原 `Viewer.vue` 的 `meta-bar`（尺寸·帧数·bit·光度·slope/intercept）已上移至此处，Viewer 内部不再单独显示元信息条。
> 状态栏布局顺序：文件名（可下拉） → 尺寸 · 帧数 → 「更多信息」按钮 → 弹性占位。

---

## 7. 关于对话框（About Dialog，v1.1 新增）

点击菜单「帮助 → 关于」弹出，模态对话框（遮罩 + 居中卡片）。

| 字段 | 取值 | 说明 |
|------|------|------|
| 软件图标 | `src/assets/about-icon.png`（取自 `src-tauri/icons/128x128.png`） | 左侧图标 |
| 软件中文名 | 医学影像处理软件 | 标题（`h2`） |
| 软件英文名 | Unixel | 中文名下方副标题（`.en-name`，`--fg-dim` 弱化、`letter-spacing:0.1em`） |
| 版本 | `getVersion()`（Tauri API） | 读取 `tauri.conf.json` 的 `version`，非硬编码 |
| 作者 | 邵宏伟 | 固定文本 |
| 联系方式 | hongweishao@outlook.com | 固定文本 |

---

## 8. 标签详情对话框（Tag Detail Dialog，v1.1 新增）

点击状态栏「更多信息」按钮，调后端 `file_tags(path)` 返回该文件的标签信息，模态对话框展示（含搜索框，可按 Tag / 关键字 / 值过滤）。

`file_tags` 按文件类型返回不同内容（`FileTags.kind`）：
- **DICOM**：遍历全部数据元素，每行 `{ tag: "(gggg,eeee)", vr, keyword, value, description }`，其中 `keyword` 经标准字典 `dicom-dictionary-std` 反查，`description` 为**标签含义解释 + 常见取值**（后端 `tag_explanation(keyword)` 释义表；`dicom-dictionary-std` 标准字典无标签名 name 字段，未收录的关键字回退为 `VR xx（VR 中文含义）`）；像素数据元素 `(7FE0,0010)` 的值以「像素数据已省略」占位，避免超大载荷。
- **NIfTI**：返回体数据头字段（尺寸、数据类型、体素大小、斜率/截距等）。
- **图像 / HTJ2K**：尽力读取格式头（尺寸、色彩类型、位深、文件大小等）；HTJ2K 复用解码路径得到基础信息。

对话框为可滚动表格，列：Tag（/索引）、VR、关键字、值、操作；顶部搜索框实时过滤行。

### 8.1 标签行单行显示与展开（v1.2 新增）
- **默认单行**：值列（`.tags-table td.val`）使用 `white-space:nowrap; overflow:hidden; text-overflow:ellipsis; max-width:240px`，整行只显示一行，过长值以省略号截断。
- **溢出检测**：通过 `valRefs`（值单元格 `:ref`）在 `filteredTags` 变化后 `nextTick` 中 `measureOverflow()` 比较 `scrollWidth > clientWidth`，得到 `overflowMap` 标记哪些行溢出。
- **展开/收起按钮**：当且仅当某行溢出（`overflowMap[tag]`）或已展开（`expandedTags.has(tag)`）时，行尾「操作」列显示「展开」（点击后该值列加 `.expanded` 类，`white-space:normal; word-break:break-all`，可换行完整查看）/"收起"切换按钮，点击 `toggleTag(tag)` 在 `expandedTags` 集合中增删。
- **操作列钉住（v1.3 新增）**：操作列（`.tags-table th:last-child, td:last-child`）设 `position:sticky; right:0; background:var(--panel); border-left:1px solid var(--border)`，对话框宽度不足、表格出现横向滚动时，「展开/收起」按钮始终钉在右侧可见区，不会因值列溢出而被挤出可视范围。
- 搜索过滤、切换图像重新打开对话框时均重置 `expandedTags` 与 `detailsQuery`，并重新测量溢出。

### 8.2 标签说明「?」、关闭「×」与导出（v1.5 新增）

**关键字说明「?」图标**
- 每行「关键字」单元格后紧跟一个圆形「?」图标（`.qmark`，`border-radius:50%` 描边小圆 + 居中问号）；仅当 `row.description` 非空时渲染（`v-if="r.description"`）。
- **仅 DICOM 显示**：`file_tags` 仅对 DICOM 行填充 `description`（含义解释 + 常见取值），NIfTI / 图像 / HTJ2K 行的 `description` 为空字符串，前端故不渲染问号。
- **hover 说明（v1.7 升级）**：鼠标悬停「?」时弹出 `.qtip` 浮层，含三段——关键字（`.qtip-key`）、说明（`.qtip-desc`，即 `description`，现为该标签的**含义解释与常见取值**，便于快速理解字段语义）、当前值（`.qtip-val`，即 `value`）；纯 CSS `:hover` 显隐，无需 JS。

**关闭「×」**
- 原对话框底部的「关闭」文字按钮移除，改为右上角「×」按钮（`.modal-close-x`，绝对定位右上、字号加大、hover 变 `--accent`）；点击效果与原「关闭」一致（置 `detailsOpen=false`）。

**导出（v1.7 调整）**
- 对话框头部仅保留「导出」按钮（移除原格式 `select`）：点击 → `save()` 系统保存框，过滤器为 `JSON(*.json)` 与 `CSV(*.csv)` 两个选项，用户在系统框的「文件类型」下拉中选择格式 → 调后端 `export_tags({ path, format, rows })`（按所选扩展名推断 `format`（`*.csv`→csv，其余→json），传完整 `rows`）→ 写入用户指定路径。
- 导出中 `exportingTags` 禁用按钮并显示 `导出中…`，结果以 `.export-tags-msg` 提示「已导出：{path}」或错误。
- `csv` 由后端 `tags_to_csv` 生成（字段 `tag,vr,keyword,value,description`，含双引号/逗号/换行转义）；`json` 由 `serde_json::to_string_pretty` 生成。

---

## 9. MPR 多平面重建视图（NIfTI）

NIfTI 体数据走独立渲染分支（`niftiRender` computed）：将 `[x][y][z]` 体按轴重排为多帧 `frames` 后复用 `Viewer`。

- **MPR 切换栏**（`.mpr-bar`，仅 NIfTI 激活时显示）：轴向 / 冠状 / 矢状 三按钮（`active` 高亮当前轴）、右侧显示体尺寸 `nx × ny × nz`。
- 切换轴 → `niftiView.axis` 变化 → `niftiKey` 变化 → `Viewer` `:key` 变化 → 重挂载并重排帧。
- NIfTI 的 `meta` 由 `extractNifti()` 结果 + `huMin/huMax` 构造（`windowCenter=(huMin+huMax)/2`、`windowWidth=huMax-huMin`）。

---

## 10. 批量导出流程

1. 点「文件 → 批量导出…」 → 弹出**多选**文件框（同 §4 过滤器）。
2. 选中后弹出**目录**选择框（"选择批量导出目录"）。
3. 调后端 `batch_export({paths, format, quality, outDir})`，`batchBusy` 期间菜单项禁用、文案"导出中…"。
4. 返回 `BatchResult{ok, failed[]}` → 菜单栏下方绿色横幅展示成功数、失败数（含失败清单 `path—error`）与"关闭"按钮。

---

## 11. 视觉规范（Theme Tokens）

来自 `src/style.css` 的 `:root` CSS 变量，深色主题：

| Token | 值 | 用途 |
|-------|-----|------|
| `--bg` | `#0d1117` | 页面/画布外背景 |
| `--panel` | `#161b22` | 菜单栏、右侧工具栏、状态栏、导出区背景 |
| `--border` | `#30363d` | 分隔线/边框 |
| `--fg` | `#e6edf3` | 主文字/文件名 |
| `--fg-dim` | `#8b949e` | 副标题/标签/提示/元信息 |
| `--accent` | `#2f81f7` | 主操作按钮、激活态、导出按钮、滑块 accent |

通用排版：
- 字体：`-apple-system, "Segoe UI", "Microsoft YaHei", sans-serif`。
- 按钮：透明底 + `--border` 描边、圆角 6px、12–13px；主操作为 `--accent` 实底白字。
- 滑块控件：12px 标签 + 滑杆 + 固定最小宽数值（窗位 44px、质量 28px 右对齐）。
- 画布：黑底（`#000`），强调灰度影像对比。
- 对话框：遮罩 + 居中卡片（`--panel` 背景、--border 边框），关于/详情共用模态样式。

状态色（横幅/提示）：
- 成功：`#2e9e5b`（导出成功文案）
- 失败/错误：`#e5484d`（导出失败、错误提示）
- 批量横幅底：`#16331f`、文字 `#c8f0d4`、失败清单 `#e5a3a3`

---

## 12. 交互与导航流程

```
[文件→打开文件] ──选择──▶ 解码(loading) ──成功(替换列表)──▶ Viewer(查看/调窗/导出)
                         │                                    │
                         └──失败──▶ 占位态红字错误
[文件/占位态→从文件夹导入] ──选目录──▶ scan_folder_series(递归扫描+Study/Series 两级分组)
        │
        ├─ 仅 1 个 Series ──▶ load_series_files(仅该序列) ──替换列表──▶ Viewer + 状态栏按系列分组下拉
        └─ 多个 Series ──▶ 序列选择对话框(按 Study 分组,行内显示序列日期/患者姓名)
                                │ 选定
                                └─▶ load_series_files(仅所选序列) ──替换列表──▶ Viewer
[状态栏下拉(optgroup)] ──选文件──▶ (未加载则懒加载 load_*) ──切换 activeId──▶ openSeq++ ──▶ Viewer 重挂载
[查看态多帧→画布右侧帧滚动条] ──点击帧号──▶ frameIndex=帧号 ──▶ 重绘
[文件→批量导出] ──多选+选目录──▶ batch_export(导出中) ──▶ 结果横幅
[右侧工具栏→导出当前帧] ──▶ export_frame(按当前窗) ──▶ 保存框
[状态栏→更多信息] ──▶ file_tags ──▶ 标签详情对话框(单行/可展开/可搜索/「?」含义说明/「×」关闭/导出)
[标签详情→导出] ──▶ save()(系统框选 JSON/CSV) ──▶ export_tags(按扩展名推断格式,写文件) ──▶ 提示已导出路径
[帮助→关于] ──▶ 关于对话框(图标/名称/版本/作者/邮箱)
[标签详情→× / 关闭] ──▶ detailsOpen=false ──▶ 回到查看态
```

- 单文件查看与批量导出共用文件菜单入口，互不影响。
- 任意时刻只有一个查看器（DICOM/图像/HTJ2K 走激活项 `view`，NIfTI 走 `niftiRender`）。
- 多图下拉仅在从文件夹导入 ≥2 张时出现；单张"打开文件"独占显示。

---

## 13. 已知限制与后续演进

- **仅未压缩 DICOM**：压缩传输语法（JPEG2000 等）暂未支持，打开会走后端错误提示。
- **批量导出为"每文件默认窗"**：导入/状态栏已支持按 `SeriesInstanceUID` 分组与排序（v1.5），但批量导出流程仍无逐文件窗预览/逐系列窗设置。
- **窗映射在 CPU（前端 JS）**：超大幅面（4K×4K）有性能压力，后续可下放 Rust/WASM 或 GPU。
- **示例数据（mock）**仅用于前端联调，不可导出。
- **"从文件夹导入"已支持递归子目录**（v1.7）：按 `StudyInstanceUID → SeriesInstanceUID` 两级聚合；非 DICOM（PNG/JPG/TIFF/HTJ2K）归入「其他影像文件」分组；NIfTI 仍走「打开文件」单文件入口，文件夹导入暂不包含 NIfTI。
- 自适应/可访问性：当前为固定桌面布局，未做移动端适配与键盘可达性（滑块已可键盘操作，但画布缩放/平移依赖鼠标）。

---

## 附录 A：关键数据模型（`src/types.ts`）

```ts
interface DicomMeta {
  path: string; filename: string;
  width: number; height: number; frames: number;
  bitsStored: number;
  pixelRepresentation: number; // 0=unsigned, 1=signed
  slope: number; intercept: number;
  windowCenter: number; windowWidth: number;
  photometric: string;          // MONOCHROME1/2, RGB…
  huMin: number; huMax: number; // 滑块边界，已含 Modality LUT
}
interface DicomImage { meta: DicomMeta; pixelBytes: Uint8Array | string | number[]; }
interface NiftiMeta  { path: string; filename: string; dims: [number,number,number]; huMin: number; huMax: number; }
interface NiftiVolume { meta: NiftiMeta; voxelBytes: Uint8Array | string | number[]; }

// v1.1 新增：已导入图像概要（多图下拉 / 状态栏用，不携带像素）
// v1.5 扩展：series_* 系列与位置字段（仅 DICOM 有值，其它类型全 None/undefined）
interface ImageInfo {
  path: string; filename: string;
  kind: string;          // "dicom" | "image" | "htj2k"
  width: number; height: number; frames: number;
  bitsStored: number; photometric: string;
  // —— v1.5 系列/位置字段（list_folder_images 已按 series_group 分组排序，单文件打开时由 file_series_info 填充）——
  seriesUid?: string | null;       // SeriesInstanceUID
  seriesNumber?: number | null;    // SeriesNumber
  modality?: string | null;       // Modality
  instanceNumber?: number | null; // InstanceNumber
  sliceLocation?: number | null;  // SliceLocation
  imagePosPatient?: number[] | null;   // ImagePositionPatient（3 分量）
  imageOrientation?: number[] | null;  // ImageOrientationPatient（6 分量：行/列方向余弦）
  seriesGroup?: number | null;     // 同系列相同（后端 group_and_sort_series 计算），无系列为 null
  seriesLabel?: string | null;     // optgroup 标题，如 "CT Series 1 · …a1b2c3d4"，无系列为 null
}

// v1.1 新增：标签详情（file_tags 返回值）
// v1.5 扩展 description；v1.7 起 description 为「标签含义解释 + 常见取值」（tag_explanation 释义表，缺省回退 VR 含义），其它类型空串
interface TagRow { tag: string; vr: string; keyword: string; value: string; description: string; }
interface FileTags {
  kind: string;          // "dicom" | "nifti" | "image"
  filename: string;
  rows: TagRow[];
}

// v1.5 新增：单文件系列/位置字段（file_series_info 返回值；非 DICOM 全 None）
interface SeriesFields {
  seriesUid?: string | null;
  seriesNumber?: number | null;
  modality?: string | null;
  instanceNumber?: number | null;
  sliceLocation?: number | null;
  imagePosPatient?: number[] | null;
  imageOrientation?: number[] | null;
}

// v1.7 新增：文件夹导入序列选择对话框数据结构（scan_folder_series 返回值）
interface SeriesBrief {
  studyUid?: string | null;
  seriesUid?: string | null;
  modality?: string | null;
  seriesNumber?: number | null;
  seriesDescription?: string | null;
  patientName?: string | null;
  patientId?: string | null;
  seriesDate?: string | null;
  studyDate?: string | null;
  fileCount: number;       // 序列内文件数
  paths: string[];         // 序列内文件路径（供 load_series_files 按需读取）
}
interface StudyBrief {
  studyUid?: string | null;
  patientName?: string | null;
  patientId?: string | null;
  studyDate?: string | null;
  series: SeriesBrief[];   // 同一 Study 下的多个 Series
}
interface SeriesTree {
  studies: StudyBrief[];
  others?: SeriesBrief | null;  // 非 DICOM（image/htj2k）归入的独立分组
}

interface BatchResult { ok: number; failed: { path: string; error: string }[]; }
```

> `pixelBytes`/`voxelBytes` 经 Tauri v2 JSON IPC 时为 `number[]`（serde_bytes 退化），前端 `decodePixelBytes` 统一兼容 `number[] / Uint8Array / base64 string` 三种形态。

## 附录 B：新增后端命令（v1.1）

| 命令 | 入参 | 返回 | 用途 |
|------|------|------|------|
| `file_tags` | `{ path }` | `FileTags` | 读取文件标签：DICOM 全量标签（keyword 经标准字典反查，`description` 填**标签含义解释+常见取值**）、NIfTI 头字段、图像/HTJ2K 格式头；供状态栏「更多信息」按钮使用 |
| `list_folder_images` | `{ dir }` | `ImageInfo[]` | 扫描文件夹**顶层**影像文件，返回概要（不解码像素）；**v1.5 起**按 `SeriesInstanceUID` 分组、组内按空间位置排序、写入 `series_group`/`series_label`；**v1.7 起「从文件夹导入」改走 `scan_folder_series`/`load_series_files`，本命令保留（及既有测试）** |
| `file_series_info`（v1.5） | `{ path }` | `SeriesFields` | 单文件打开时只读 DICOM 头（不解码像素）提取系列/位置字段，填充 `ImageInfo` 系列字段；非 DICOM 全部返回 `None` |
| `scan_folder_series`（v1.7） | `{ dir }` | `SeriesTree` | 文件夹导入核心：递归遍历目录下所有支持文件，DICOM 按 `StudyInstanceUID → SeriesInstanceUID` 两级聚合（记录患者姓名/ID、检查与序列日期、模态、序列号/描述、文件数），非 DICOM 归入 `others`；供「序列选择对话框」展示 |
| `load_series_files`（v1.7） | `{ paths: string[] }` | `ImageInfo[]` | 给定所选序列的文件路径列表，构建 `ImageInfo`（复用 `folder_image_info`）并按 `group_and_sort_series` 分组排序；非 DICOM 序列统一打 `series_label="其他影像文件"`；返回后由前端替换 `imageList` 仅显示该序列 |
| `export_tags`（v1.5） | `{ path, format, rows: TagRow[] }` | `String`（写出路径） | 标签详情对话框导出：`format="csv"` 走 `tags_to_csv`（字段 `tag,vr,keyword,value,description`，含转义）；否则 `serde_json::to_string_pretty` 写入用户指定文件。`format` 由前端按保存框所选扩展名推断（v1.7 起格式选择移入系统保存框） |

> 像素数据元素在 `file_tags` 中以「像素数据已省略」占位，避免超大载荷拖垮前端渲染。
> 系列分组排序（`group_and_sort_series`）：按 `series_uid` 分组；组内排序键 `series_sort_key` 优先级为 **① `ImagePositionPatient` 沿法向量投影**（法向量 = `cross3(IOP[0..3], IOP[3..6])` 归一化）→ ② `InstanceNumber` → ③ `SliceLocation` → ④ 0.0；组间按 `(seriesNumber, modality, filename)` 排序（`None` 系列置末）。结果写入 `series_group`（同系列相同编号）/ `series_label`。
